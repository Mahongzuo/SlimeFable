# Copy to cloud/.env on the Lighthouse (gitignored). Same token as worker.config.json.
SLIME_PLAY_TOKEN=change-me
SLIME_STATUS_HOST=127.0.0.1
SLIME_STATUS_PORT=8091
SLIME_INFRA_DIR=/opt/PixelStreamingInfrastructure

# Wilbur (do not use 8888 — 宝塔 panel):
#   --streamer_port 18888
#   --player_port 18880
#   --https false
#   listen via Nginx /ps/streamer and /ps/player only
